apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.job.serviceAccount }}
  namespace: {{ .Values.job.namespace }}
  annotations:
    description: "Service account for Fleet bootstrap operations"
